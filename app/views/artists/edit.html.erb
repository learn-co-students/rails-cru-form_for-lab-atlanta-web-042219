<h1>Edit Artist Form</h1>

<%= form_for(@artist) do |f| %>
    <label>Artist Name:</label><br>
    <%= f.text_field :name, placeholder: @artist.name %><br>

    <label>Artist Bio:</label><br>
    <%= f.text_area :bio, placeholder: @artist.bio %><br>

    <%= f.submit %>
<% end %>